Write a SQL query to get Product list of twenty most expensive products:

| ProductName | UnitPrice |

Order products by price.

db.products.aggregate([
   {$project:{"ProductName":1,"UnitPrice":1}},
   {$sort:{"UnitPrice":-1}},
   {$limit:20}
])
