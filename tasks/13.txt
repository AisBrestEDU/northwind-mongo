Create a SQL query to count current and discontinued products:

| TotalOfCurrentProducts | TotalOfDiscontinuedProducts |

var totalCountProd_Disc_0= db.products.count({Discontinued:0});
var totalCountProd_Disc_1 = db.products.count({Discontinued:1});

