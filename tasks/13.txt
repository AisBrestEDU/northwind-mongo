Create a SQL query to count current and discontinued products:

| TotalOfCurrentProducts | TotalOfDiscontinuedProducts |

//task13
var TotalOfCurrentProducts = db.products.find({ Discontinued: 0}).count();
var TotalOfDiscontinuedProducts = db.products.find({ Discontinued: 1}).count();
