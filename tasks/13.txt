Create a SQL query to count current and discontinued products:

| TotalOfCurrentProducts | TotalOfDiscontinuedProducts |

var result = {'TotalOfCurrentProducts': db.products.count({Discontinued: 0}), 'TotalOfDiscontinuedProducts': db.products.count({Discontinued: 1})};
result

