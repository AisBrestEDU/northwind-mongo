Return all products where product name starts with 'A', 'B', .... 'F' ordered by name.

| ProductId | ProductName | QuantityPerUnit |

db.getCollection('products').find(
{
    ProductName:/^[A-F]/
},
{
    _id:0, ProductID:1, ProductName: 1, QuantityPerUnit:1
}
).sort({ProductName:1})
