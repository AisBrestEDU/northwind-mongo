Write a query to get discontinued Product list:
| ProductId | Name | 

db.getCollection('products').aggregate([
    {
        $match:{
            Discontinued:1
        }
    },
    {
        $project:{
            _id:0,
            ProductId: "$ProductID",
            Name: "$ProductName"
        }
    }
])
