Create a query to return:

| CategoryName | TotalNumberOfProducts |

db.categories.aggregate([
    {
      $lookup:
        {
          from: "products",
          localField: "CategoryID",
          foreignField: "CategoryID",
          as: "inventory_docs"
        }
   },
   {$project:{_id:0,'CategoryName':1,'TotalNumberOfProducts':{ $size: "$inventory_docs" }}}
])